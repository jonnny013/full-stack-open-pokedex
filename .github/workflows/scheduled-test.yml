name: Scheduled test

on:
  schedule:
    - cron: '10 * * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Do health test
        run: npm run health
      